x-app: &app
  env_file:
    - .env
    - .env.production
  build:
    context: .
    target: hyku-web
    cache_from:
      - ghcr.io/notch8/wvu_knapsack/base:latest
      - ghcr.io/notch8/wvu_knapsack:latest
    args:
      BUILDKIT_INLINE_CACHE: 1
      APP_PATH: ./hyrax-webapp
  image: ghcr.io/notch8/wvu_knapsack:${TAG:-latest}
  environment:
    - BUNDLE_LOCAL__HYKU_KNAPSACK=/app/samvera
    - BUNDLE_DISABLE_LOCAL_BRANCH_CHECK=true
    - BUNDLE_BUNDLER_INJECT__GEM_PATH=/app/samvera/bundler.d
  volumes:
    - ./data/node_modules:/app/samvera/hyrax-webapp/node_modules
    - ./data/uploads:/app/samvera/hyrax-webapp/public/uploads
    - ./data/assets:/app/samvera/hyrax-webapp/public/assets
    - ./data/cache:/app/samvera/hyrax-webapp/tmp/cache
    - .:/app/samvera

networks:
  internal:
    driver: bridge

services:
  zoo:
    env_file:
      - .env
      - .env.production
    extends:
      file: ./hyrax-webapp/docker-compose.production.yml  # FIXED PATH
      service: zoo
    volumes:
      - ./data/zoo:/data
      - ./data/zk:/datalog

  solr:
    container_name: solr
    image: ghcr.io/notch8/wvu_knapsack/solr:${TAG:-latest}
    restart: on-failure
    platform: linux/amd64
    env_file:
      - .env
      - .env.production
    user: root
    command: bash -c "sh /scripts/startup.sh hydra_prod"
    ports:
      - "8983:8983"
    volumes:
      - ./data/solr:/var/solr/data/hydra_prod/data
      - ./data/logs/dev/:/var/solr/logs
      - ./solr9-setup:/solr9-setup
      - ./startup-solr.sh:/scripts/startup.sh
    healthcheck:
      test: "curl -f localhost:8983/solr/#/"
      start_period: 30s
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      internal:

  fcrepo:
    env_file:
      - .env
      - .env.production
    extends:
      file: ./hyrax-webapp/docker-compose.production.yml  # FIXED PATH
      service: fcrepo
    volumes:
      - ./data/fcrepo:/data

  db:
    env_file:
      - .env
      - .env.production
    extends:
      file: ./hyrax-webapp/docker-compose.production.yml  # FIXED PATH
      service: db
    volumes:
      - ./data/db:/var/lib/postgresql/data

  redis:
    env_file:
      - .env
      - .env.production
    extends:
      file: ./hyrax-webapp/docker-compose.production.yml  # FIXED PATH
      service: redis
    volumes:
      - ./data/redis:/data

  web:
    <<: *app
    env_file:
      - .env
      - .env.production
    extends:
      file: ./hyrax-webapp/docker-compose.production.yml  # FIXED PATH
      service: web
    environment:
      - BUNDLE_LOCAL__HYKU_KNAPSACK=/app/samvera
      - BUNDLE_DISABLE_LOCAL_BRANCH_CHECK=true
      - VIRTUAL_PORT=3000
    ports:
      - "3000:3000"
    entrypoint: ["sh", "-c"]
    command:
      - bundle install && ./bin/web
    depends_on:
      db:
        condition: service_started
      solr:
        condition: service_healthy
      fcrepo:
        condition: service_started
      redis:
        condition: service_started
      zoo:
        condition: service_started
      worker:
        condition: service_started
      initialize_app:
        condition: service_completed_successfully

  worker:
    <<: *app
    env_file:
      - .env
      - .env.production
    extends:
      file: ./hyrax-webapp/docker-compose.production.yml  # FIXED PATH
      service: worker
    build:
      context: .
      target: hyku-worker
      cache_from:
        - ghcr.io/notch8/wvu_knapsack/base:latest
        - ghcr.io/notch8/wvu_knapsack/worker:latest
      args:
        BUILDKIT_INLINE_CACHE: 1
        APP_PATH: ./hyrax-webapp
    image: ghcr.io/notch8/wvu_knapsack/worker:${TAG:-latest}
    entrypoint: ["sh", "-c"]
    command:
      - bundle install && ./bin/worker
    depends_on:
      initialize_app:
        condition: service_completed_successfully
      db:
        condition: service_started
      solr:
        condition: service_healthy
      fcrepo:
        condition: service_started
      redis:
        condition: service_started
      zoo:
        condition: service_started
    volumes:
      - ./data/node_modules:/app/samvera/hyrax-webapp/node_modules
      - ./data/uploads:/app/samvera/hyrax-webapp/public/uploads
      - ./data/assets:/app/samvera/hyrax-webapp/public/assets
      - ./data/cache:/app/samvera/hyrax-webapp/tmp/cache
      - .:/app/samvera

  # check_volumes runs as a no-op stub so that web/worker depends_on constraints
  # inherited from the submodule via 'extends' are satisfied. The real chown
  # operations are skipped here because the ./data/* host paths are already
  # owned correctly on the production host.
  check_volumes:
    <<: *app
    env_file:
      - .env
      - .env.production
    user: root
    entrypoint: ["sh", "-c"]
    command:
      - "echo 'check_volumes skipped in production' && exit 0"
    networks:
      - internal

  initialize_app:
    <<: *app
    env_file:
      - .env
      - .env.production
    extends:
      file: ./hyrax-webapp/docker-compose.production.yml  # FIXED PATH
      service: initialize_app
    command:
      - >
        solrcloud-upload-configset.sh /app/samvera/hyrax-webapp/solr/conf &&
        solrcloud-assign-configset.sh &&
        SOLR_COLLECTION_NAME=hydra-test solrcloud-assign-configset.sh &&
        bundle install &&
        db-migrate-seed.sh


