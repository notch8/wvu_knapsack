# redis container only — do not add Rails app vars here.
# REDIS_PASSWORD must also match the password in REDIS_URL in .env.production.
# Format: REDIS_URL=redis://:PASSWORD@redis:6379/0
REDIS_PASSWORD=changeme
