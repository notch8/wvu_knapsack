# postgres container only — do not add Rails app vars here.
# These map directly to the official postgres image environment variables.
# POSTGRES_PASSWORD must also match DB_PASSWORD in .env.production
# and the fcrepo.postgresql.password in .env.fedora.
POSTGRES_PASSWORD=changeme
POSTGRES_USER=postgres
POSTGRES_DB=hyku
