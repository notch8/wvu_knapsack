<%#
  OVERRIDE: Hyku 6.2.0 to center the download PDF button with constrained width to match WVU show theme design,
  rather than having it span the full page width as in the base Hyku implementation. 
  
  Changes made for WVU show theme styling:
  - Removed `btn-block` class to prevent full-width button spanning the page
  - Removed `center-block` class (Bootstrap 3, deprecated)
  - Added `mx-auto` and `d-block` classes (Bootstrap 4/5 utilities) for centered, non-full-width button
  - Added `text-center` class to wrapper div for container-level centering
  - Custom CSS in wvu_show_buttons.scss applies min-width: 350px, max-width: 500px for proper sizing
%>
<% if can?(:download, file_set_id) %>
  <div class="download-pdf-controls text-center">
    <% if @presenter.representative_presenter.present? && presenter.file_set_presenters.any?(&:pdf?) %>
      <%= button_tag type: 'button',
                     id: "file_download",
                     data: { label: @presenter.representative_presenter.id,
                             path: hyrax.download_path(presenter.representative_presenter) },
                     class: "btn btn-success file_download mx-auto d-block",
                     onclick: "window.open(this.dataset.path, '_blank');" do %>
        Download PDF
      <% end %>
    <% end %>
  </div>
<% end %>

